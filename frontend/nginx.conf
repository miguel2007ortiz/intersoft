server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Archivos Angular (SPA) — todas las rutas caen en index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy reverso: todas las llamadas a /api van al backend Django
    location /api/ {
        proxy_pass         http://backend:8000;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }

    # Archivos estaticos de Django (si los sirve nginx)
    location /static/ {
        proxy_pass http://backend:8000;
    }

    # Archivos media de Django
    location /media/ {
        proxy_pass http://backend:8000;
    }

    # Desactivar el cache para los hashes de Angular
    location ~* \.(?:css|js)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Gzip
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript image/svg+xml;
    gzip_min_length 256;
}
