# Configuracion de bandit para el pipeline de CI (C2).
# Se excluyen hallazgos que son falsos positivos o intencionales:
#   - B608: las consultas SQL de analytics.py SIEMPRE usan %s parametrizado;
#           la unica interpolacion es `where` construida desde codigo controlado.
#   - B403: el cache de Django usa pickle de forma intencional (datos internos).
#   - B310: urlopen de ia_engine.py apunta a settings.IA_API_URL (config, no input).
#   - B311: random de seed_masivo solo para imagenes de relleno (no cripto).
#   - B105/B106/B107: secretos de DEV (settings placeholder) y passwords del seed.
#
# La exclusion de tests se hace via -x en el workflow (bandit no filtra por
# archivo en este config file).

skips:
  - B608
  - B403
  - B310
  - B311
  - B105
  - B106
  - B107
