# =====================================================================
#  InterSoft - dependencias de DESARROLLO del backend (CI y local).
#  No se instalan en produccion. Incluye linters de calidad y cobertura
#  usados por el pipeline de CI (workflow .github/workflows/ci.yml).
#  Instalacion:  pip install -r requirements-dev.txt
# =====================================================================

ruff==0.8.4
bandit==1.8.3
coverage==7.6.9
